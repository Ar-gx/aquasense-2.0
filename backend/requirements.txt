fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0,<2.1
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27
scikit-learn>=1.4
xgboost>=2.0
pandas>=2.2
numpy>=1.26
joblib>=1.3
# PostgreSQL driver — only used when DATABASE_URL is set (Render / Neon).
# SQLite (the local default) needs no driver.
psycopg2-binary>=2.9.9
